<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gallery</title>
    <link rel="stylesheet" href="/main.css">
</head>

<body>
  
    <main>
        <div class="gallery">
            <% for(let i = 0; i < pokedex.length; i++) { %>
                <div class="movie-card">
                    <figure>
                        <img src="<%= pokedex[i].sprite %>" alt="pokemon"/>
                        <figcaption>
                            <h1><%= pokedex[i].name %></h1>
                            <p><%= pokedex[i].types %></p>
                        </figcaption>
                    </figure>
                </div>
            <% } %>
    </main>

</body>